server:
  port: 9001

spring:
  profiles: 
    active: local
  application:
    name: wms-admin
  datasource: 
    url: jdbc:mysql://localhost:3306/wop
    username: root
    password: ENC(uQlNcXwafbkFVHVbLnVihVnv/RK4nz55)
    driver-class-name: com.mysql.cj.jdbc.Driver

eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka
      #defaultZone: http://eureka7001.com:7001/eureka/,http://eureka7002.com:7002/eureka/,http://eureka7003.com:7003/eureka/
    register-with-eureka: false

feign:
  hystrix:
    enabled: false
  httpclient:
    enabled: true #Feign默认使用的连接工具实现类，所以里面发现只要你有body体对象，就会强制的把GET请求转换成POST请求

#ribbon的超时时间，feign客户端底层使用ribbon作为客户端负载均衡
ribbon:
  ReadTimeout: 6000 #Feign调用Read timed out，默认1秒即1000
  ConnectTimeout: 6000    

jwt: 
  secret: Z3kyMDIw
  expiration: 604800 #60*60*24
  tokenHeader: Authorization
  tokenHead: Bearer
  
jasypt: 
 encryptor: 
  password: guiyuan2020