<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlsrj.wms.saas.mapper.TenantBookMapper">
	<select id="selectAggregation" resultType="com.zlsrj.wms.api.entity.TenantBook">
		SELECT
			count(id) as id,
			sum(book_read_cycle) as book_read_cycle,
			sum(book_settle_cycle) as book_settle_cycle,
			sum(price_calss) as price_calss
		FROM tenant_book
		${ew.customSqlSegment} 
	</select>
</mapper>